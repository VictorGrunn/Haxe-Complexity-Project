package overmap.optionscreen;
import overmap.ScreenTemplate;

/**
 * ...
 * @author Victor Grunn
 * 
 * NOTE: Generalize this so we can re-use the same options in both the main screen and this screen. Convert everything to a particular template?
 */
class OptionScreenPiece extends ScreenTemplate
{

	public function new() 
	{
		super();
	}
	
}